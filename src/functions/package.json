{
  "name": "az-demand-vpn-functions",
  "version": "1.0.0",
  "description": "Azure Functions for on-demand WireGuard VPN lifecycle management",
  "scripts": {
    "start": "func start",
    "test": "NODE_OPTIONS='--localstorage-file=/tmp/jest-localstorage' jest --coverage --runInBand",
    "test:watch": "NODE_OPTIONS='--localstorage-file=/tmp/jest-localstorage' jest --watch --runInBand"
  },
  "dependencies": {
    "@azure/arm-containerinstance": "^9.0.0",
    "@azure/functions": "^4.0.0",
    "@azure/identity": "^4.0.0",
    "@azure/keyvault-secrets": "^4.8.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
